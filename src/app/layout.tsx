import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Lumina | Free AI Spend Audit for Startups",
  description: "Identify 30%+ savings in your AI tool stack. Get an instant, defensible audit of your spend on Cursor, Claude, OpenAI, and more.",
  keywords: ["AI spend audit", "startup savings", "Cursor pricing", "Claude pricing", "OpenAI costs", "Credex"],
  authors: [{ name: "Credex Entrepreneurial Intern" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
